# Required for draft preview (unpublished content).
# Create at https://www.sanity.io/manage → Project → API → Tokens
# Use a token with Viewer role (or custom) that can read drafts on dataset "summit".
SANITY_READ_TOKEN=

# Required in production: shared secret for /preview links from Sanity Studio
SANITY_PREVIEW_SECRET=

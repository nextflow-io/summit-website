---
import Layout from "@layouts/Base.astro";
import PastEvents from "@modules/PastEvents";
import Contact from "@modules/Contact";
import { SponsoredBy } from "@components/Marquee";
import SEO from "@components/SEO";
import Button from "@components/Button";
import { Image } from "astro:assets";
import clsx from 'clsx';

import styles from "./_styles.module.css";
import * as galleryImages from "./images";


---

<Layout namespace="2024/barcelona" altBG>
  <SEO slot="head" title="Gallery Summit Barcelona 2024" img={1} bcn />

<section class="pb-6 pt-16 md:pt-20">
    <div class="container island text-center mb-12">
      <h2 class="h000 mb-10">Photo Gallery</h2>
      <h3 class="h2 mb-10 text-nextflow">Nextflow Summit Barcelona 2024</h3>
      <p>See photos from the nf-core hackathon and Nextflow Summit.</p>
      <div class="mt-10">
        <Button 
          to="https://photos.google.com/share/AF1QipNTi1ns-yYu_ut8597MJqybdu8boTKJ28nFgpFRcOyRQrMBf6F7_xk2abKz-QcaGw?key=VTV0OHRhRUt0UnFtNnNSYzBVM0RsM0NPUmhvVHFR" 
          arrow 
          cta 
          wide
        >
          See all photos
        </Button>
      </div>
    </div>

    <div class={`container ${styles.masonryGrid}`}>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img1}
          alt="nf-core Hackathon"
          class={`${styles.galleryImage} h-[300px]`}
          loading="lazy"
          width={600}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img12}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[350px]")}
          loading="lazy"
          width={600}
          height={400}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img3}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[170px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img16}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[300px]")}
          loading="lazy"
          width={600}
          height={200}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img5}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[200px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img6}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[341px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img7}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[300px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img8}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[280px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img9}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[400px]")}
          loading="lazy"
          width={600}
          height={600}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img10}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[230px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img11}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[250px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img2}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[240px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img13}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[300px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img14}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[220px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img4}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[300px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
      <div class={`${styles.masonryItem} ${styles.fadeIn}`}>
        <Image
          src={galleryImages.img15}
          alt="nf-core Hackathon"
          class={clsx(styles.galleryImage, "h-[300px]")}
          loading="lazy"
          width={600}
          height={300}
          quality={80}
        />
      </div>
    </div>

    <div class="mt-12 text-center">
      <Button 
        to="https://photos.google.com/share/AF1QipNTi1ns-yYu_ut8597MJqybdu8boTKJ28nFgpFRcOyRQrMBf6F7_xk2abKz-QcaGw?key=VTV0OHRhRUt0UnFtNnNSYzBVM0RsM0NPUmhvVHFR" 
        arrow 
        cta 
        wide
      >
        See more photos in Google Photos
      </Button>
    </div>
  </section>

  <Contact className="py-8" />
  <SponsoredBy client:load location="barcelona" />
</Layout>


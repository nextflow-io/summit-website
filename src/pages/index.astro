---
import Layout from '@layouts/Base.astro';
import SEO from '@components/SEO';
import Banner from '@components/Banner';
import Homepage from "@modules/Homepage"
import { fetchHomepage } from '@data/fetchers';
import type { HomepageData } from '@data/types';

const previewMode = Astro.cookies.get('sanity-preview')?.value === 'true';
console.log('Preview mode:', previewMode); // Check this in terminal

const home: HomepageData = await fetchHomepage();
---

<Layout>
  <SEO slot="head" title="Home" />
  <!-- <Banner title="Register to access the event recordings" ctaText="Register Now" ctaLink="https://seqera.swoogo.com/nextflow-summit-boston-2025" client:load />  -->
  <Homepage home={home}  client:load/>
</Layout>

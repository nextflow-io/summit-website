---
import Layout from '@layouts/Base.astro';
import SEO from '@components/SEO';
import StandardPage from '@modules/StandardPage';
import { fetchBostonTravel } from '@data/fetchers';
import type { PageData } from '@data/types';
import LandingHero from '@components/LandingHero';

const travel: PageData = await fetchBostonTravel();
---

<Layout>
  <SEO slot="head" title="Agenda | Boston" />
  <div class="bg-black text-white">
    <section class="bg-black text-white w-full">
    <div class="container-xl py-10">
        <h1 class="h3 mb-4 md:mb-8 max-w-[800px]">Agenda</h1>
        <p>Check back for an update schedule of events. </p>
    </div>
  </section>
  </div>
</Layout>

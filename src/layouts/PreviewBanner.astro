---
const previewMode = Astro.cookies.get('sanity-preview')?.value === 'true';
console.log('🎨 PreviewBanner rendering, previewMode:', previewMode);
---

{previewMode && (
  <div style="
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background: red;
    color: white;
    padding: 20px;
    text-align: center;
    z-index: 99999;
    font-size: 24px;
    font-weight: bold;
  ">
    🚨 PREVIEW MODE ACTIVE 🚨
    <a href="/api/exit-preview" style="color: yellow; margin-left: 20px; text-decoration: underline;">
      EXIT PREVIEW
    </a>
  </div>
)}